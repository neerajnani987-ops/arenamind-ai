# The port number on which the Express server will listen (e.g. 5000)
PORT=5000
# The environment mode, e.g. development or production
NODE_ENV=production
# The Google Gemini API key used to query the GoogleGenerativeAI model (optional, falls back to local simulation if not set)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
